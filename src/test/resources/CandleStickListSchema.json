{
  "$id": "localhost:8080/api/v1/balance/history",
  "type": "array",
  "definitions": {},
  "$schema": "http://json-schema.org/draft-07/schema#",
  "items": {
    "$id": "localhost:8080/api/v1/balance/history/items",
    "type": "object",
    "properties": {
      "open": {
        "$id": "localhost:8080/api/v1/balance/history/items/properties/open",
        "type": "number",
        "title": "The Open Schema ",
        "default": 0.0,
        "examples": [
          0
        ]
      },
      "close": {
        "$id": "localhost:8080/api/v1/balance/history/items/properties/close",
        "type": "number",
        "title": "The Close Schema ",
        "default": 0.0,
        "examples": [
          0
        ]
      },
      "high": {
        "$id": "localhost:8080/api/v1/balance/history/items/properties/high",
        "type": "number",
        "title": "The High Schema ",
        "default": 0.0,
        "examples": [
          0
        ]
      },
      "low": {
        "$id": "localhost:8080/api/v1/balance/history/items/properties/low",
        "type": "number",
        "title": "The Low Schema ",
        "default": 0.0,
        "examples": [
          0
        ]
      },
      "volume": {
        "$id": "localhost:8080/api/v1/balance/history/items/properties/volume",
        "type": "number",
        "title": "The Volume Schema ",
        "default": 0.0,
        "examples": [
          0
        ]
      },
      "timestamp": {
        "$id": "localhost:8080/api/v1/balance/history/items/properties/timestamp",
        "type": "integer",
        "title": "The Timestamp Schema ",
        "default": 0,
        "examples": [
          1528470000
        ]
      }
    }
  }
}